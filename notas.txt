-> git init

-> git status

-> git add . ou git add nome_arquivo.html

-> git commit -m" "

-> git log

-> git commit -am" " - dá o add e o commit junto

-> CTRL + L = dá clear na tela

Quando dá o log com + de 3, entra no vue, editor de texto no linux

-> Q - quit desses dois pontos

-> ALT + Z = quebra linhas no html;

-> Criar parágrafo antes - lorem + número de palavras = texto de preenchimento;

-> CTRL + B = esconde a barra lateral ou mostra

-> No controle de código fonte, apertar +, depois escrever a msg, daí é adicionado como commit.

---------------------------------------------------------

->CTRL + SHIFT + DELL -> tira o histórico do google

Copiar as três linhas no novo repositório:

-> git branch -M main - alterando o nome da ramificação inicial - o github só funciona com main, que é a linha principal

-> git remote add origin https://github.com/octlui/primeiro-rep.git - ex. para conectar no servidor remoto

-> git remote

-> git push - é enviar

-> git pull